# third_party

`third_party/ragflow` is intentionally not tracked in this repository.

Run:

```powershell
.\scripts\bootstrap-ragflow.ps1
```

The script clones the selected RAGFlow tag into `third_party/ragflow`. For deep secondary development, replace the clone remote with your own RAGFlow fork and keep `infiniflow/ragflow` as upstream.
