diff --git a/api/apps/services/models_api_service.py b/api/apps/services/models_api_service.py
--- a/api/apps/services/models_api_service.py
+++ b/api/apps/services/models_api_service.py
@@ -70,2 +70,7 @@ def _is_local_openai_compatible(provider_name: str, base_url: str | None) -> bo
     )
 
 
+def _is_local_vlm_endpoint(base_url: str | None) -> bool:
+    normalized = (base_url or "").lower()
+    return "localmathrag-vlm" in normalized or ":8083" in normalized
+
+
@@ -75,7 +80,13 @@ def _is_local_openai_compatible(provider_name: str, base_url: str | None) -> bo
-def _runtime_kind_for_model_type(model_type: str) -> str | None:
+def _runtime_kind_for_model_type(
+    model_type: str,
+    base_url: str | None = None,
+    shared_chat_vision: bool = False,
+) -> str | None:
     normalized = MODEL_TAG_TO_TYPE.get(model_type, model_type)
+    if normalized == "chat" and shared_chat_vision and _is_local_vlm_endpoint(base_url):
+        return "vision"
     if normalized in {"chat", "embedding", "rerank", "asr", "tts"}:
         return normalized
@@ -94,6 +105,14 @@ def _instance_base_url(instance_obj) -> str:
         extra = {}
     return str(extra.get("base_url") or "")
 
+
+def _shares_chat_and_vision_default(tenant, provider_name: str, instance_name: str, model_name: str, model_type: str) -> bool:
+    if model_type not in {"chat", "vision"}:
+        return False
+    other_field = "img2txt_id" if model_type == "chat" else "llm_id"
+    expected = f"{model_name}@{instance_name}@{provider_name}"
+    return getattr(tenant, other_field, None) == expected
+
 
 def _switch_local_runtime_model_if_needed(
     tenant_id: str,
@@ -103,11 +122,12 @@ def _switch_local_runtime_model_if_needed(
     instance_name: str,
     model_name: str,
     model_type: str,
+    shared_chat_vision: bool = False,
 ) -> dict | None:
-    kind = _runtime_kind_for_model_type(model_type)
-    if not kind or not model_name:
+    if not model_name:
         return None
 
     provider_obj = TenantModelProviderService.get_by_tenant_id_and_provider_name(tenant_id, provider_name)
@@ -121,11 +141,13 @@ def _switch_local_runtime_model_if_needed(
     base_url = _instance_base_url(instance_obj)
     if not _is_local_openai_compatible(provider_name, base_url):
         return None
+    kind = _runtime_kind_for_model_type(model_type, base_url, shared_chat_vision)
+    if not kind:
+        return None
 
     payload = {
         "kind": kind,
         "model": model_name,
-        "start": kind in {"chat", "rerank"},
+        "start": kind in {"chat", "rerank"} or (kind == "vision" and shared_chat_vision),
         "timeout_seconds": float(os.getenv("LOCALMATHRAG_RUNTIME_SWITCH_TIMEOUT_SECONDS", "240")),
     }
@@ -385,11 +407,19 @@ def set_tenant_default_models(tenant_id: str, model_provider: str, model_instan
         return False, "model_provider, model_instance and model_name must be specified together"
 
     if default_model:
+        shared_chat_vision = _shares_chat_and_vision_default(
+            tenant,
+            model_provider,
+            model_instance,
+            model_name,
+            model_type,
+        )
         switch_result = _switch_local_runtime_model_if_needed(
             tenant_id,
             model_provider,
             model_instance,
             model_name,
             model_type,
+            shared_chat_vision,
         )
         if isinstance(switch_result, dict) and not switch_result.get("ok", True):
             return False, f"Local runtime model switch failed: {switch_result.get('reason') or switch_result.get('detail') or switch_result}"
diff --git a/web/src/pages/user-setting/setting-model/components/un-add-model.tsx b/web/src/pages/user-setting/setting-model/components/un-add-model.tsx
--- a/web/src/pages/user-setting/setting-model/components/un-add-model.tsx
+++ b/web/src/pages/user-setting/setting-model/components/un-add-model.tsx
@@ -135,5 +135,8 @@ const getPrimaryType = (
 const getPrimaryType = (
   model: Pick<LocalModel | RecommendedModel, 'model_type'>,
-): ModelGroup => normalizeModelType(model.model_type?.[0]);
+): ModelGroup =>
+  model.model_type?.some((type) => type === 'vision' || type === 'image2text')
+    ? 'vision'
+    : normalizeModelType(model.model_type?.[0]);
 
 const instanceNameByType: Record<ModelGroup, string> = {
@@ -159,10 +162,17 @@ const makeDefaults = (
   >,
 ): LocalModelDefaults => {
   const primaryType = getPrimaryType(model);
+  const modelTypes = Array.from(
+    new Set(
+      (model.model_type || []).flatMap(
+        (type) => ragflowTypeByGroup[normalizeModelType(type)],
+      ),
+    ),
+  );
   return {
     instance_name: instanceNameByType[primaryType],
     // Persist the same provider-facing identifier for every model type.
@@ -172,7 +182,7 @@ const makeDefaults = (
     // repository path when it needs to replace a container model.
     model_name: model.name,
-    model_type: ragflowTypeByGroup[primaryType],
+    model_type: modelTypes.length ? modelTypes : ragflowTypeByGroup[primaryType],
     base_url: model.base_url,
     api_key: 'local',
     max_tokens: model.max_tokens,
